---

- name: ubuntu_codename | resolve codename
  ansible.builtin.set_fact:
    ubuntu_codename: "{{ ubuntu_codename_mapping[ansible_distribution][ansible_distribution_release] | default(ansible_distribution_release) }}"

